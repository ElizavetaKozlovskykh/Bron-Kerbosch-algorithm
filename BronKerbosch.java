import java.util.*;
public class BronKerbosch {
   /* private static Set<Integer> compsub;
    private static Set<Integer> candidates;
    private static Set<Integer> not;
    private static Set<Integer> max_compsub;
    public static Set<Integer> GetBronKerbosch(Graph G, Set<Integer> candidates, Set<Integer> not)
    {
      for (int j=0; j<G.GetNumVertex(); j++)
      {
          candidates.add(j);
      }

        boolean check=true;
        int v=0;
        Set<Integer> noncontiguous;

        while (candidates.isEmpty()&check)
        {
            while(!candidates.contains(v)){v++;}
            compsub.add(v);
            for (int j=0; j<G.GetNumVertex(); j++)
            {
                if ()
            }
            candidates.removeAll(noncontiguous);
            not.removeAll(noncontiguous);
            if (candidates.isEmpty()&not.isEmpty()) {
                if (max_compsub.size() < compsub.size()) max_compsub = compsub;
            }
            else GetBronKerbosch(G, candidates, not);
            compsub.remove(v);
            candidates.remove(v);
            not.add(v);
        }

        return max_compsub;
    }*/

    }

